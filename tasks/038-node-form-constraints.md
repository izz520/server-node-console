# 任务记录：节点表单基础约束

## 任务状态

已完成

## 任务背景

外部节点手动添加和分享链接导入是 `PRD.md` 的核心流程。后端已经校验节点名称、协议、地址和端口，但前端部分输入框缺少基础 HTML 约束，用户可能提交空字段或非法端口后才看到接口错误。

## 本次任务目标

为节点相关表单补齐浏览器基础校验。

## 本次调整范围

### 前端

- 外部节点名称、地址必填。
- 外部节点端口和监听端口限制 `1-65535`。
- 分享链接导入的链接必填。
- 系统安装节点名称必填已保留，服务器/协议/端口约束继续保留。

## 不包含范围

- 不做协议专属字段校验。
- 不解析分享链接前端预校验。

## 验收标准

- 手动添加外部节点时空名称/地址无法直接提交。
- 端口输入框限制合法范围。
- 分享链接为空无法直接提交。
- `pnpm check`、`pnpm build` 通过。

## 风险与待确认

- 浏览器原生校验只做基础约束，最终仍以后端校验为准。

## 完成记录

- 节点手动表单已有名称、地址、端口、监听端口基础约束。
- 分享链接导入已有必填约束。
- 本次补充手动/系统安装协议下拉 `required` 约束，保持表单一致性。

## 验证结果

- `pnpm check` 通过。
- `pnpm build` 通过。

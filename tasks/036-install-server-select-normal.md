# 任务记录：安装表单仅选择正常服务器

## 任务状态

已完成

## 任务背景

后端已经要求只有 `normal` 状态服务器可以发起协议安装。前端安装表单仍展示所有服务器，用户可能选择连接失败或禁用服务器后才看到接口错误。

## 本次任务目标

安装表单中只将正常服务器作为可选项，并提示用户需要先完成 SSH 连通性测试。

## 本次调整范围

### 前端

- 安装表单服务器下拉只展示 `normal` 状态服务器。
- 没有正常服务器时显示提示。
- `InstallNodeFields` 类型补充服务器状态字段。

## 不包含范围

- 不自动测试 SSH。
- 不改变服务器页面状态逻辑。

## 验收标准

- 连接失败/禁用服务器不会出现在安装可选项中。
- 无正常服务器时用户看到提示。
- `pnpm check`、`pnpm build` 通过。

## 风险与待确认

- 用户需要回到服务器页面手动修复连通性后再安装协议。

## 完成记录

- 安装表单服务器下拉已仅展示 `normal` 状态服务器。
- 无正常服务器时会显示 SSH 连通性测试提示。
- `InstallNodeFields` 类型已补充服务器状态字段。

## 验证结果

- `pnpm check` 通过。
- `pnpm build` 通过。

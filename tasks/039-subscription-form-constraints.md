# 任务记录：订阅表单基础约束

## 任务状态

已完成

## 任务背景

`PRD.md` 中订阅名称、订阅格式、包含节点列表为必填。当前前端通过保存按钮禁用控制节点列表，但订阅名称和格式仍需要显式表单约束。

## 本次任务目标

为订阅表单补齐基础 HTML 约束，减少无效请求。

## 本次调整范围

### 前端

- 订阅名称输入框增加 `required`。
- 订阅格式下拉增加 `required`。

## 不包含范围

- 不新增复杂字段级校验。
- 不改变节点选择交互。

## 验收标准

- 空订阅名称无法直接提交。
- 订阅格式下拉显式必填。
- `pnpm check`、`pnpm build` 通过。

## 风险与待确认

- 节点列表仍通过保存按钮禁用和后端校验共同保证。

## 完成记录

- 订阅名称输入框已有 `required` 约束。
- 订阅格式下拉已补充 `required` 约束。

## 验证结果

- `pnpm check` 通过。
- `pnpm build` 通过。

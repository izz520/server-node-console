# 任务记录：使用新服务器复测 AnyTLS 自动安装

## 任务状态

已完成

## 任务背景

前一台服务器上手动执行 argosbx AnyTLS 安装可以成功，说明后端自动安装链路需要在真实服务器上重新验证。当前后端已增加远端依赖引导、SSH 执行超时、argosbx 输出失败识别和 SSH 流式任务日志。

## 本次任务目标

使用用户提供的新服务器，通过后端 API 完成服务器添加、AnyTLS 自动安装、任务状态轮询和日志查看，验证系统自动安装链路是否可用。

## 本次调整范围

### 验证

- 启动本地后端临时服务。
- 通过 API 注册临时测试用户。
- 添加用户提供的新服务器并触发 SSH 连通性测试。
- 发起 AnyTLS 自动安装任务。
- 轮询任务状态和任务日志。
- 如果安装成功，记录节点信息；除非需要清理，不主动卸载。

## 不包含范围

- 不把 SSH 密码写入仓库文件或任务文档。
- 不修改产品代码，除非验证暴露必须修复的问题。
- 不主动删除用户服务器上的成功安装结果。

## 验收标准

- 后端能添加服务器并完成 SSH 连通性验证。
- 后端能发起 AnyTLS 自动安装任务。
- 任务日志能显示远端脚本输出。
- 最终任务状态为 `success` 或能明确记录失败原因。

## 执行结果

- 已通过本地后端 API 注册临时测试用户并添加新服务器。
- SSH 连通性验证通过，服务器状态为 `normal`。
- 已发起 AnyTLS 自动安装任务。
- 任务日志成功实时输出 argosbx 安装过程。
- 安装任务最终状态为 `success`。
- 节点最终状态为 `install_success`。
- AnyTLS 监听端口为 `29318`。
- argosbx 输出显示 Sing-box `1.13.11` 运行中，节点配置已生成。
- 临时后端服务已停止，临时 E2E 脚本已删除。

## 备注

- 本次验证使用临时测试用户创建数据，节点记录属于该临时用户账号。
- 真实服务器上的 AnyTLS 节点当前已安装成功；如需纳入正式账号管理，可以在正式账号中导入服务器上 `/root/agsbx/jhsub.txt` 的分享链接，或卸载后通过正式账号重新安装。

# 任务记录：本地启动文档更新

## 任务状态

已完成

## 任务背景

当前 `README.md` 和 `backend/README.md` 仍保留脚手架阶段描述，例如“多数接口返回 501”。实际项目已经实现用户、服务器、节点、订阅、任务、管理员等核心能力，启动文档需要同步更新，避免后续本地启动和排错混淆。

## 本次任务目标

更新本地启动说明、数据库说明和当前功能状态，让文档匹配当前实现。

## 本次调整范围

### 文档

- 更新根 `README.md`。
- 更新 `backend/README.md`。
- 说明 `.env` 自动加载规则。
- 说明 Postgres 数据库需要先存在，后端会自动迁移表结构。
- 说明常用验证命令。

## 不包含范围

- 不新增部署文档。
- 不调整 Docker Compose 配置。
- 不修改运行脚本。

## 验收标准

- 文档不再描述接口为 501/占位。
- 用户能按文档启动前端和后端。
- 文档明确数据库创建与自动迁移边界。

## 风险与待确认

- 本地 Postgres 用户名因机器不同而不同，文档提供 Docker 和本机 Postgres 两种路径。

## 完成记录

- 根 `README.md` 已更新为当前系统说明。
- `backend/README.md` 已更新后端启动、数据库和接口入口说明。
- 已明确“后端自动迁移表结构，但不会自动创建数据库本身”。
- 已补充 Docker Compose 和本机 PostgreSQL 两种启动路径。
- 已补充前后端验证命令。

## 验证结果

- 文档更新，无需执行编译验证。

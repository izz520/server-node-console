# 任务记录：真实服务器 SSH 连通性验收

## 任务状态

已完成

## 任务背景

用户提供了一台测试服务器的 SSH 信息，希望用于验证第一期真实服务器链路。第一步应先做 SSH 连通性验收，确认系统可以连接服务器；协议安装/卸载会真实修改远端服务器，需要在单独确认协议后执行。

## 本次任务目标

使用提供的测试服务器信息验证 SSH 连通性，并尽量通过现有后端接口或 SSH 客户端逻辑完成真实链路测试。

## 本次调整范围

### 验证

- 不在任务文档中记录 SSH 密码明文。
- 测试 SSH 主机、端口、用户名、密码是否可连接。
- 不执行安装脚本。
- 不修改远端服务器配置。

## 不包含范围

- 不安装 sing-box 协议。
- 不执行 argosbx 安装/卸载脚本。
- 不持久化测试凭据到额外文档。

## 验收标准

- 可以明确判断 SSH 是否可连接。
- 如果失败，记录失败原因。
- 如果成功，再进入真实安装/卸载验收任务。

## 风险与待确认

- 网络访问可能需要运行环境授权。
- 真实协议安装前需要确认要安装的协议和是否允许卸载清理。

## 完成记录

- 已使用项目后端同款 `sshclient.TestConnection` 逻辑验证真实服务器 SSH 连通性。
- 验证过程中未执行安装脚本。
- 验证过程中未修改远端服务器配置。
- 临时测试程序已删除。
- SSH 密码未写入任务文档或仓库文件。

## 验证结果

- SSH 连通性通过：`SSH_CONNECT_OK`。
